should = require('should')

Main = require('../lib/main')

describe 'API', ->
  it 'should authenticate requests'

  describe 'common endpoints', ->
    it 'submit job'
    it 'request job'
    it 'finish job'
    it 'fail job'
